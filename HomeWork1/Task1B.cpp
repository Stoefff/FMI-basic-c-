#include <iostream>

using namespace std;

// Good example for bit operations: https://developer.android.com/reference/android/graphics/Color.html
// int color = (A & 0xff) << 24 | (R & 0xff) << 16 | (G & 0xff) << 16 | (B & 0xff);
int main(){

    return 0;
}
